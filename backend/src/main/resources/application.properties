# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/api

# Ensure context path is properly registered
spring.mvc.servlet.path=/

# Allow access to actuator endpoints
management.endpoints.web.base-path=/actuator
management.endpoints.web.exposure.include=health,info,metrics
# Application Properties
app.contact.email=ayushraj12121212@gmail.com

# Database Configuration
spring.datasource.url=jdbc:h2:mem:portfolio_db
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Spring Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ayushraj12121212@gmail.com
spring.mail.password=tduxbjfzxgtvadki
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging
logging.level.org.springframework=INFO
logging.level.com.portfolio=DEBUG

# Validation Messages
spring.messages.basename=messages/validation

# Allow binding for nested objects
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# CORS Configuration
cors.allowed-origins=https://portfolio-orcin-zeta-43.vercel.app,http://localhost:3000
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true
